spring:
  datasource:
    url: jdbc:mysql://mysql:3306/user_db
    username: ${DB_USERNAME}
    password: ${DB_PASSWORD}

jwt:
  secret: ${JWT_SECRET}

cookie:
  same-site: Strict
  secure: true