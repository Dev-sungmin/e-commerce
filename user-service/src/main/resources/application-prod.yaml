spring:
  datasource:
    url: jdbc:mysql://mysql:3306/user_db
    username: ${DB_USERNAME}
    password: ${DB_PASSWORD}
    hikari:
      maximum-pool-size: 5

jwt:
  secret: ${JWT_SECRET}

cookie:
  same-site: Strict
  secure: true