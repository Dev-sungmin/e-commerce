spring:
  datasource:
    url: jdbc:mysql://mysql:3306/new_payment
    username: ${DB_USERNAME:root}
    password: ${DB_PASSWORD:root}

order:
  base-url: ${ORDER_BASE_URL:http://order-service:8081}